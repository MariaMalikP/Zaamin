import React from 'react';

const ProfileHome = (props) => {
  // const { email } = props.match.params;
  
  // Use the email parameter as needed
  
  return (
    <div>
      <h2>Welcome. Profile Home</h2>
      {/* Other content */}
    </div>
  );
};

export default ProfileHome;